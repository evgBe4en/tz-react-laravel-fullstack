import React from "react";

const Calendarr = () => {
    const getHeader = () => {};
    const getWeekDaysNames = () => {};
    const getDates = () => {};

    return (
        <section>
            {getHeader()}
            {getWeekDaysNames()}
            {getDates()}
        </section>
    );
};

export default Calendarr
